# Us - A Shared Memory Journal (我们的故事)

> 💖 **GitHub Repository**: [https://github.com/lin-hongkuan/us](https://github.com/lin-hongkuan/us)

![PWA Ready](https://img.shields.io/badge/PWA-Ready-purple?style=flat-square&logo=pwa)
![React](https://img.shields.io/badge/React-18-blue?style=flat-square&logo=react)
![Vite](https://img.shields.io/badge/Vite-5.0-646CFF?style=flat-square&logo=vite)
![Supabase](https://img.shields.io/badge/Supabase-Enabled-3ECF8E?style=flat-square&logo=supabase)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-3.4-38B2AC?style=flat-square&logo=tailwindcss)

**Us** 是一个专为情侣设计的共享回忆日记应用。它提供了一个温馨、私密的空间，让“她”和“他”可以分别记录各自视角的点点滴滴，共同编织属于两个人的美好回忆。

现已支持 **PWA (Progressive Web App)**，您可以将其安装到手机或电脑桌面，享受原生应用般的体验。

![Preview](doc/image.png)

## ✨ 特性 (Features)

### 📱 渐进式 Web 应用 (PWA)
- **可安装**: 支持添加到手机主屏幕或电脑桌面，拥有独立图标和启动页。
- **离线访问**: 即使在网络不佳的情况下,也能查看已缓存的页面框架。
- **沉浸式体验**: 隐藏浏览器地址栏,全屏运行,提供原生 App 般的操作手感。

### 🎨 沉浸式视觉体验
- **双色调主题**:
  - 💗 **Her Side**: 温暖的玫瑰粉色调 (Rose),象征温柔与细腻。
  - 💙 **His Side**: 清新的天空蓝色调 (Sky),象征宽广与包容。
  - 🌈 **融合过渡**: 桌面端采用分屏融合,移动端采用平滑渐变切换。
- **动态流光背景**:
  - 背景色彩随时间缓慢流动 (`animate-gradient`)。
  - **交互式光影 (Desktop)**: 鼠标悬停在不同区域时，背景色调会以极具质感的阻尼动画缓慢过渡，营造出呼吸般的沉浸感。
  - **趣味交互**: 点击屏幕任意位置（包括登录页）都会迸发出可爱的小星星。
  - 移动端优化了性能,减少高斯模糊以保证流畅度。
- **精致排版**:
  - 标题采用 **Skranji** 字体,俏皮可爱。
  

### 💝 核心功能
- **双视角记录**: 支持分别以“她”或“他”的身份发布回忆。
- **📸 照片上传**: 每条回忆可以附带一张照片，记录美好瞬间。
  - 保持原图质量，不压缩不缩放。
  - 支持下载照片到本地。
  - 支持 Supabase Storage（推荐，1GB 免费空间）或 Base64 存储。
- **回忆管理**: 支持随时修改、删除回忆内容或照片。
- **时间轴展示**: 按时间倒序排列，清晰展示两人的共同历程。
- **数据持久化**:
  - 支持 **Supabase** 云端存储(推荐,可跨设备同步)。
  - 支持 **LocalStorage** 本地存储(自动降级，照片以 Base64 格式保存)。

## 🛠️ 技术栈 (Tech Stack)

- **前端框架**: [React](https://react.dev/) (v18+)
- **构建工具**: [Vite](https://vitejs.dev/)
- **PWA 支持**: [vite-plugin-pwa](https://vite-pwa-org.netlify.app/)
- **样式库**: [Tailwind CSS](https://tailwindcss.com/)
- **字体**: Google Fonts (Skranji, Ma Shan Zheng, Playfair Display)
- **后端/存储**: [Supabase](https://supabase.com/) (PostgreSQL)

## 🚀 快速开始 (Getting Started)

### 1. 克隆项目
```bash
git clone https://github.com/lin-hongkuan/us.git
cd us
```

### 2. 安装依赖
```bash
npm install
```

### 3. 启动开发服务器
```bash
npm run dev
```
访问 `http://localhost:5173` 即可预览。

### 4. 构建生产版本
```bash
npm run build
```

## 📲 PWA 安装指南

### iOS (Safari)
1. 在 Safari 中打开网站。
2. 点击底部的“分享”按钮。
3. 向下滑动并选择“添加到主屏幕”。

### Android (Chrome)
1. 在 Chrome 中打开网站。
2. 点击右上角菜单（三个点）。
3. 选择“安装应用”或“添加到主屏幕”。

### Desktop (Chrome/Edge)
1. 在浏览器地址栏右侧点击"安装"图标。
2. 确认安装后,应用将以独立窗口运行。

## 🧭 未来计划 (Roadmap)

- **多张照片 / 相册模式**: 每条回忆支持多图轮播或拼贴，上传后可拖拽排序。
- **富文本与标记**: 文本支持粗体/斜体/列表，支持标记心情、天气、小标签便签。
- **搜索与筛选**: 关键字搜索、按时间段筛选、按标签/心情过滤，快速找到某一段回忆。
- **提醒与纪念日**: 支持设置纪念日倒计时、提醒“今天也写一条吧”，并生成年度回顾。
- **导出与备份**: 一键导出为 PDF/Markdown/JSON，或备份到云端存储桶。
- **协作与权限**: 允许共享给指定邮箱/手机号，设置只读或可评论权限。
- **安全与私密**: 可选加密存储、本地加锁（如 PIN/指纹），并支持隐藏敏感回忆。
- **多语言与时区**: 国际化翻译，自动适配时区与日期格式。
- **性能与体验**: 懒加载图片、离线草稿保存、更轻量的动画开关以适配低端设备。


## 📖 使用教程 (User Guide)

### 首次使用

1. **选择身份**: 首次进入网站时，点击 💗 **Her** 或 💙 **His** 选择你的身份
2. **记录回忆**: 点击右下角的 ✏️ 按钮打开编辑器

### 创建回忆

1. 在编辑器中输入文字内容
2. **添加照片**（可选）:
   - 点击左下角的 📷 **添加照片** 按钮
   - 选择图片文件（支持 jpg、png 等格式，最大 10MB）
   - 系统会自动压缩图片以节省空间
   - 可以预览、更换或删除照片
3. 点击 **记录美好** 按钮保存

### 管理回忆

#### 编辑回忆
1. 将鼠标悬停在你发布的回忆卡片上
2. 点击右下角的 ✏️ 编辑按钮
3. 修改文字或照片
4. 点击 ✅ 保存修改

#### 删除回忆
1. 将鼠标悬停在你发布的回忆卡片上
2. 点击右下角的 🗑️ 删除按钮
3. 确认删除

#### 查看照片
- 点击回忆卡片上的照片可以放大/缩小查看

#### 下载照片
- 鼠标悬停在照片上，点击右上角的 ⬇️ 下载按钮
- 照片将以原始质量下载到本地

### 切换视角

- **桌面端**: 
  - 左侧是"她"的回忆，右侧是"他"的回忆
  - 可以同时看到双方视角
  
- **移动端**: 
  - 点击顶部的 **Her** 或 **His** 标签切换视角
  - 或者左右滑动切换

### 权限说明

- 每个人只能编辑和删除自己发布的回忆
- 可以查看对方发布的所有回忆
- 照片功能无需配置即可使用（会自动存储到 LocalStorage 或 Supabase Storage）

## 💾 数据存储说明

### Supabase 云端存储（推荐）
- **优点**: 数据云端保存，可跨设备访问
- **存储限制**: 
  - 数据库 500MB（免费版）
  - 照片存储 1GB（免费版）
- **需要**: 配置 Supabase 项目

### LocalStorage 本地存储（备用）
- **优点**: 无需配置，即开即用
- **照片存储**: 以 Base64 格式保存原图
- **缺点**: 
  - 数据仅保存在当前浏览器
  - 清除浏览器数据会丢失所有内容
  - Base64 格式会比原图大约 37%
  - 浏览器 LocalStorage 通常限制 5-10MB
  - 大图片可能存储失败
- **适用**: 临时测试或单设备使用

> **💡 提示**: 即使不配置 Supabase，照片功能也可以正常使用，会自动保存到 LocalStorage！

## 🗄️ 后端配置 (Supabase Setup)

本项目默认支持 LocalStorage，但为了数据持久化和照片存储，**强烈建议配置 Supabase**。

### 步骤 1: 创建项目并配置数据库

1. 登录 [Supabase](https://supabase.com/) 创建一个新项目。
2. 进入项目的 **SQL Editor**，运行 `supabase_setup.sql` 中的脚本以创建表结构和安全策略。
   - **重要**: 务必运行完整的 SQL 脚本，特别是 `Enable update access` 策略，否则无法修改回忆。

### 步骤 2: 配置 Storage（照片存储）

#### 2.1 创建 Storage Bucket
1. 在 Supabase 控制台左侧菜单点击 **Storage**
2. 点击 **New bucket**
3. 输入名称：`memory-images`
4. **勾选** "Public bucket"（让图片可以公开访问）
5. 点击 **Create bucket**

#### 2.2 设置访问策略
点击刚创建的 `memory-images` bucket，然后点击 **Policies** 标签，创建以下 3 个策略：

**策略 1: 允许上传**
- Policy name: `Allow public uploads`
- Allowed operation: ✅ INSERT
- Policy definition: `bucket_id = 'memory-images'`

**策略 2: 允许读取**
- Policy name: `Allow public read`
- Allowed operation: ✅ SELECT
- Policy definition: `bucket_id = 'memory-images'`

**策略 3: 允许删除**
- Policy name: `Allow public delete`
- Allowed operation: ✅ DELETE
- Policy definition: `bucket_id = 'memory-images'`

### 步骤 3: 配置项目密钥

1. 在 Supabase 项目设置中获取 `URL` 和 `anon key`
2. 打开 `src/services/storageService.ts`，填入你的配置：

```typescript
const SUPABASE_URL = 'your-project-url';
const SUPABASE_KEY = 'your-anon-key';
```

完成后重启开发服务器，照片上传功能即可正常使用！

## 📂 项目结构 (Project Structure)

```
.
├── src/                    # 源代码目录
│   ├── components/         # React 组件
│   │   ├── Composer.tsx    # 编辑器组件
│   │   ├── MemoryCard.tsx  # 回忆卡片组件
│   │   └── TypewriterText.tsx
│   ├── services/           # 业务逻辑服务
│   │   └── storageService.ts
│   ├── App.tsx             # 主应用组件
│   ├── index.tsx           # 入口文件
│   ├── index.css           # 全局样式
│   └── types.ts            # TypeScript 类型定义
├── public/                 # 静态资源
│   └── icon.png            # 应用图标
├── doc/                    # 文档资源
├── scripts/                # 辅助脚本
│   └── export_memories.js
├── index.html              # HTML 入口
├── supabase_setup.sql      # 数据库初始化脚本
├── tailwind.config.js      # Tailwind CSS 配置
├── vite.config.ts          # Vite & PWA 配置
├── tsconfig.json           # TypeScript 配置
└── package.json            # 项目依赖
```

## 🌐 部署 (Deployment)

### GitHub Pages
本项目已配置好相对路径 (`base: './'`)，非常适合部署到 GitHub Pages。

1. 构建项目：`npm run build`
2. 将 `dist` 文件夹的内容推送到 `gh-pages` 分支,或直接在 GitHub 仓库设置中将 Pages 源设置为 `main` 分支的 `/root` (如果直接上传构建产物)。
3. **推荐方式**: 使用 GitHub Actions 自动部署。

## 🤝 贡献 (Contributing)

欢迎提交 Issue 或 Pull Request 来改进这个项目!无论是修复 Bug 还是添加新功能,我们都非常感谢。

## 📄 许可证 (License)

MIT License
